name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - run: python -m pip install --upgrade pip==25.2
      - run: pip install ".[dev]"
      - run: ruff check .
      - run: mypy app tests
      - run: pytest

  frontends:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        frontend: [app, web]
    defaults:
      run:
        working-directory: apps/${{ matrix.frontend }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/${{ matrix.frontend }}/package-lock.json
      - run: npm ci
      - run: npm run check

  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cp .env.example .env
      - run: docker compose config --quiet
      - run: docker compose build
